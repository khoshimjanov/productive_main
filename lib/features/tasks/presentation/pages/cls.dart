
import 'dart:ui';

import 'package:flutter_svg/flutter_svg.dart';

class Containers{
  String? title;
  String? text;
  String? time;
  String? icon;
  bool? isLink;
  Color? color;
  bool? check;
  Containers({required this.title,required this.text,required this.time,required this.icon,required this.isLink,required this.color,required this.check});
}